# Notes on Running Program
To run the program with the example graph run "python main.py ./ex_graph.txt" in the command line when in this project's file directory.
You also have multiple options to 'view' 'clear' and access the algorithms with 'ls X', 'dv X' or 'dv'.
These commands are also listed when the program is run. 

Thanks!